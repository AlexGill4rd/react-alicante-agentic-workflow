# F2 — Schedule search & filters

## Acceptance

- Text search across title, speaker, description, room
- Filter by track and level
- Empty state when no matches
- Works on `/sessions` without breaking the timeline layout
